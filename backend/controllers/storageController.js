// TODAY: Local storage
exports.uploadFile = async (filePath) => {
    // filePath = "uploads/aadhaar/12345.png"
    return filePath; // just return path for now
  };
  
  // FUTURE (AWS S3 or Cloudinary):
  // Replace this entire function with cloud upload logic